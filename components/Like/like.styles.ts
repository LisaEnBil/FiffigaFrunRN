import { StyleSheet } from 'react-native';

export const likeStyles = StyleSheet.create({
  likeContainer: {
    width: 44,
    height: 44,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
